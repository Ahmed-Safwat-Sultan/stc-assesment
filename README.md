# stc-assesment

note that the following points are intentionally skipped:

- check for uniqueness fully qualified names (from application side)
- checking for file existence (binary hashing)
- getting the userEmail input as an authenticated input e.g. from a token
- handling users login
- some complexities were ignored related to the tree structure of the files, folders and spaces
- using professional committing and branching with git